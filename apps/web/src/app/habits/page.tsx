import { AppShell } from "@/components/shell/app-shell";

export default function HabitsPage() {
    return (
        <AppShell menuId={2}>
            <h1>Habits</h1>
            <p>Welcome to the Habits page!</p>
        </AppShell>
    );
}